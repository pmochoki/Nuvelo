# Copy to `.env` for local tooling (optional). App reads `assets/env` + dart-define.
# Match web `VITE_SUPABASE_*` from Vercel / web `.env.local`.
SUPABASE_URL=https://ahiujuljjbozmfwoqtli.supabase.co
SUPABASE_ANON_KEY=
